import * as CryptoJs from 'crypto-js';

// Components
import { localstore } from './defineObj';

// export const getUser = () => {
//     const descryptUser = localStorage.getItem(localstore.USER_LOGIN);
//     const jsonUser = CryptoJs.AES.decrypt(descryptUser, localstore.scretkey).toString(CryptoJs.enc.Utf8);
//     const user = JSON.parse(jsonUser);
//     return user;
// }
